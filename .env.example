# Copy to .env only on the machine that runs the demo. Never commit .env.
# The app must remain usable through its deterministic fallback when unset.
PITT_AI_BASE_URL=
PITT_AI_API_KEY=
PITT_AI_MODEL=
