{
  "name": "pitt-build-week-demo",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 4173 --directory app",
    "demo:runs": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
